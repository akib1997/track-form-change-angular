import { TrackFormChangeDirective } from './track-form-change.directive';

describe('TrackFormChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackFormChangeDirective();
    expect(directive).toBeTruthy();
  });
});
